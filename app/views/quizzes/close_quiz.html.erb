<div class="container mt-4">
  <div class="row">
    <div class="col-12">
      <% text = @quiz.finish? ? "Reopen Quiz #{@quiz.name}" : "Close quiz #{@quiz.name}" %>
      <%= link_to text, quiz_path(@quiz), method: :patch, class: "btn btn-primary" %>
    </div>
  </div>
</div>
